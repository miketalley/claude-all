# Simple Todo App

Build a basic command-line todo application that allows users to:
- Add tasks
- List all tasks
- Mark tasks as complete
- Delete tasks

The application should store tasks in a JSON file for persistence.

## Requirements
- Use Node.js
- Simple CLI interface
- Tasks should have: id, title, completed status
- Data persists between sessions
